const { Typography } = require("@mui/material");

const RedirectPage = () => {
  return (
    <>
      <Typography variant="h4" m={5}>REDIRECT HOME PAGE</Typography>
    </>
  );
};

export default RedirectPage;
